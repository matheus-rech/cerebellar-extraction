{
  "mcpServers": {
    "genkit-cerebellar": {
      "command": "node",
      "args": ["./node_modules/.bin/genkit", "mcp"],
      "timeout": 60000,
      "trust": true,
      "env": {
        "NODE_ENV": "production"
      }
    },
    "firebase": {
      "command": "npx",
      "args": [
        "-y",
        "firebase-tools@latest",
        "mcp",
        "--dir", "/Users/matheusrech/root/cerebellar-extraction",
        "--only", "auth,firestore,storage,functions"
      ],
      "timeout": 60000,
      "trust": true
    }
  }
}
